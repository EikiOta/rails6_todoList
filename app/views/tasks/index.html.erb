<h1>一覧画面</h1>
<%= link_to '投稿', "/tasks/new" %>

<table>
  <tr>
    <th>タスク名</th>
    <th>登録日時</th>
  </tr>
  <% @tasks.each do |task| %>
  <tr>
    <td><%= link_to task.name, task_path(task) %></td>　
    <td><%= task.created_at %></td>
  </tr>
  <% end %>

</table>